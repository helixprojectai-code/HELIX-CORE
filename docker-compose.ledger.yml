version: '3.8'

services:
  helix-ledger:
    image: python:3.11-slim
    container_name: helix_ledger
    working_dir: /app
    volumes:
      - ./helix-ledger:/app
    command: tail -f /dev/null
    networks:
      - helix-net

networks:
  helix-net:
    driver: bridge
